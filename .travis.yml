language: bash
services: docker
script:
  - docker-compose build
before_deploy:
  - echo "${DOCKER_PASSWORD}" | docker login --username "${DOCKER_USERNAME}" --password-stdin
deploy:
  provider: script
  script:
    docker push tiagosp13/mysql-api-any;
    docker push tiagosp13/api-any;
  on:
    branch: master
notifications:
  email: false
